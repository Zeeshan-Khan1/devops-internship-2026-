version: "3.9"
services:
  web:
    build: .
    ports:
      - "5000:5000"
    networks:
      - app-net
    depends_on:
      - redis
  redis:
    image: redis:alpine
    volumes:
      - redis-data:/data
    networks:
      - app-net

networks:
  app-net:

volumes:
  redis-data:
  